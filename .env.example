# Local testing configuration
# Copy this file to .env.local and customize for your local environment

# Local testing flags
LOCAL_TESTING=true
SKIP_DYNAMODB_CHECK=true
PROMPT_CONTEXT_STORAGE=file

# Temporal configuration for local development
TEMPORAL_SERVER_URL=localhost:7233
TEMPORAL_NAMESPACE=default
TEMPORAL_TASK_QUEUE=investigate-task-queue
TEMPORAL_IDENTITY=local-worker

# ============================================================================
# CLAUDE API / SUBSCRIPTION TOGGLE
# ============================================================================
#
# This system supports TWO modes for accessing Claude:
#
# MODE 1: API Key Mode (USE_CLAUDE_CLI=false) [DEFAULT]
#   - Uses your Claude API key directly
#   - Requires: ANTHROPIC_API_KEY environment variable
#   - Best for: notebookllm integration, API-based usage, production
#   - Get your key at: https://console.anthropic.com/
#
# MODE 2: CLI Subscription Mode (USE_CLAUDE_CLI=true)
#   - Uses your Claude Code subscription via the CLI
#   - Requires: Claude CLI installed and authenticated
#   - Best for: Development, testing when you have a subscription
#   - Install: npm install -g @anthropic-ai/claude-code
#   - Authenticate: claude login
#
# TOGGLE BETWEEN MODES: Set USE_CLAUDE_CLI to true or false below
#
# ============================================================================

# Claude Mode Selection
# Set to 'false' for API mode (uses ANTHROPIC_API_KEY)
# Set to 'true' for CLI/subscription mode (uses Claude CLI, no API key needed)
USE_CLAUDE_CLI=false

# API Key (required when USE_CLAUDE_CLI=false)
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=

# CLI Timeout (only used when USE_CLAUDE_CLI=true)
# Time in seconds to wait for CLI requests (default: 300 = 5 minutes)
CLAUDE_CLI_TIMEOUT=300

# Optional: Override repository settings for testing
# GITHUB_TOKEN=your-github-token-here
GITHUB_TOKEN=

# Architecture Hub Configuration (for local testing)
ARCH_HUB_REPO_NAME=repo-swarm-sample-results-hub
ARCH_HUB_BASE_URL=https://github.com/example
# empty means root
ARCH_HUB_FILES_DIR=

# Repository Configuration (for local testing)
DEFAULT_ORG_NAME=royosherove
DEFAULT_REPO_URL=https://github.com/sindresorhus/is

# Git Configuration (for local testing)
GIT_USER_NAME="Architecture Bot"
GIT_USER_EMAIL=archbot@example.com

# Optional: Local DynamoDB settings (if testing with local DynamoDB)
# AWS_ACCESS_KEY_ID=local
# AWS_SECRET_ACCESS_KEY=local
# AWS_REGION=us-east-1
# DYNAMODB_ENDPOINT_URL=http://localhost:8000

# Optional: Debug settings
# LOG_LEVEL=DEBUG
# PYTHONUNBUFFERED=1
